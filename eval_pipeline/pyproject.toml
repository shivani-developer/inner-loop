[project]
name = "eval_pipeline"
version = "0.1.0"
description = "Off-device LLM-judge scoring and Pareto reporting for the journaling companion."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
